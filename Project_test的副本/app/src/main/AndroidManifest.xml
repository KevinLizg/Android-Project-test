<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project_test">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ImageReview"></activity>
        <activity
            android:name=".Imageview"
            android:label="Picture View" />
        <activity
            android:name=".password"
            android:label="Password Store" />
        <activity android:name=".GestureLock" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".User"
            android:label="USER" />
        <activity
            android:name=".Register"
            android:label="REGISTER" />
        <activity
            android:name=".Resetpwd"
            android:label="RESET" />
        <activity
            android:name=".ImageChooser"
            android:label="Choose Image" />
    </application>

</manifest>